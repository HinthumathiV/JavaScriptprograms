//1. FACTORIAL OF A NUMBER
function calculateFactorial(num) {
    let result = 1;
 for (let i = 2; i <= num; i++) {
   result  *= i;
 }
 return result;
    
 }
 console.log(calculateFactorial(5));


 //2.Printing sum of n Natural Numbers [For Loop]:
 // //loop from i= 1 to i=n
 //in each iteration, i is increased by 1
  
 let sum= 0;
 const n =100;
for (let i = 0; i <=n; i++) {
   sum +=i;    //sum=sum+i
  
 }
 console.log("sum is:" + sum);

// //3.Sum of only Positive Numbers:[While Loop]

//    let num=0,sum1=0;
//    while (num >= 0) {
//     sum1+= num;
//   //Taking input from the User
   
//   num = parseInt(prompt("Enter a number: "));
// }
//  console.log("The sum is:" + sum1);

//4.Square root of a number
  const number1=9;
  const number2=2.56;

  const result = Math.sqrt(number1);
  const result2 =Math.sqrt(number2);

  console.log(`The square root of ${number1} is ${result}`)
  console.log(`The square root of ${number2} is ${result2}`)

  //5.Area of a triangle:
const baseValue = prompt('Enter the base of a triangle: ');
const heightValue = prompt('Enter the height of a triangle: ');

// calculate the area
const areaValue = (baseValue * heightValue) / 2;

console.log(`The area of the triangle is ${areaValue}`);